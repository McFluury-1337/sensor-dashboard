# BRIEF: Этап 7 — сдача
Дата: 2026-09-22

## Формулировка своими словами
Этап 7 по заданию требует: README с инструкцией «развернуть с нуля», REPORT по каждому этапу (уже есть — `harness/stage-N/REPORT.md` за все шесть этапов), заполненные `DECISIONS.md`/`LESSONS.md` (уже заполнены), и пять минут демонстрации куратору. Критерий проверки жёсткий: куратор сам разворачивает проект по README, ни о чём не спрашивая — если не получилось у него, README не готов.

Но у этого этапа есть внешняя зависимость, которую я закрыть не могу: пункт «ссылка работает, данные живые» и требование Этапа 6 «сайт живёт минимум неделю до сдачи» упираются в то, что вы ещё не прошли Часть B (хостинг на Beget) — аккаунта там пока нет. Поэтому в этом заходе я закрываю всё, что не зависит от хостинга, и явно фиксирую, что ещё не готово к сдаче.

## Контекст
- README сейчас — три строки (только питч, без инструкций). Практические команды запуска раньше жили в `harness/PROJECT.md`, но куратор явно просит именно README.
- Журналы (`DECISIONS.md` — 11 записей, `LESSONS.md` — 2 записи) уже в порядке, REPORT есть по каждому из Этапов 1–6 (Часть A) плюс отдельный RUNBOOK для Части B Этапа 6.
- Только что прошёл ревизию кода: убрал мелкое дублирование и неконсистентные имена переменных в `app.py` (`t/p/v` → `temperature/pressure/vibration`), дополнил один непрозрачный комментарий контекстом. Секретов в репозитории не нашёл (`.env` не в git, тестовый пароль MariaDB в `tests/test_db_mysql.py` — не боевой секрет, а локальные тестовые данные, которые разработчик заводит себе сам).
- Живого хостинга ещё нет — пункт «ссылка работает» из критериев сдачи и недельный аптайм из Этапа 6 не могут быть закрыты в этом заходе.

## Критерии успеха
- [ ] README содержит: что за проект (кратко), как запустить локально с нуля (клонировать → venv → зависимости → `.env` → `python app.py` → `python simulator.py` → `pytest`), как задеплоить на Beget (кратко, со ссылкой на полный `harness/stage-6/RUNBOOK.md`), ссылки на журналы и per-stage отчёты
- [ ] Инструкция локального запуска из README реально проверена — клонирую репозиторий в чистую временную директорию и прохожу шаги буквально, а не полагаюсь на то, что «должно сработать»
- [ ] Секретов в репозитории нет (проверено `git grep` по всем реальным значениям из `.env`)
- [ ] `pytest` зелёный (уже 36/36, перепроверю после README)
- [ ] Честно зафиксировано в README/чате, что для полной сдачи ещё не хватает: живого хостинга (Часть B Этапа 6) и недели аптайма

## Открытые вопросы
Нет продуктовых развилок — README это факт-чекнутый пересказ того, что уже сделано и задокументировано по этапам. Единственная неопределённость — сам хостинг — уже явно вынесена в раздел «Что дальше», а не скрыта.

## Чек-лист выхода
- [x] формулировка своими словами есть — с явной оговоркой про незакрытую зависимость от Этапа 6
- [x] критерии успеха измеримы
- [x] открытые вопросы закрыты (продуктовых развилок нет, внешняя зависимость явно названа)
